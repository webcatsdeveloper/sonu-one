{% comment %}
  SONU Specifications Section 
{% endcomment %}

<section class="sonu-specs-section specs-id-{{ section.id }}"
  style="
    background-color: {{ section.settings.bg_color }};
    {% if section.settings.bg_image != blank %}
      background-image:url('{{ section.settings.bg_image | image_url: width: 2400 }}');
    {% endif %}
  ">

  <div class="page-width">

    <div class="specs-main-grid">

      <div class="specs-media-col animate-blur-spec">
        {% if section.settings.left_image != blank %}
          <img
            src="{{ section.settings.left_image | image_url: width: 1800 }}"
            alt="{{ section.settings.heading | escape }}"
            class="specs-display-img"
            loading="lazy"
          >
        {% endif %}
      </div>

      <div class="specs-content-col">

        {% if section.settings.heading != blank %}
          {% assign formatted_heading = section.settings.heading
            | replace: '[[', '<span class="specs-highlight">'
            | replace: ']]', '</span>'
            | newline_to_br
          %}
          {%- assign head_tag = section.settings.heading_style | default: 'h2' -%}
          <{{ head_tag }} class="specs-main-title typography-{{ head_tag }} animate-blur-spec delay-spec-1">
            {{ formatted_heading }}
          </{{ head_tag }}>
        {% endif %}

        <div class="specs-features-grid">
          {% for block in section.blocks %}
            <div class="specs-feature-item animate-blur-spec delay-spec-{{ forloop.index }}">

              <div class="specs-item-header">
                {% if block.settings.icon_svg != blank or block.settings.icon_image != blank %}
                  <div class="specs-icon-wrapper">
                    {% if block.settings.icon_svg != blank %}
                      {{ block.settings.icon_svg }}
                    {% elsif block.settings.icon_image != blank %}
                      <img
                        src="{{ block.settings.icon_image | image_url: width: 100 }}"
                        alt=""
                        loading="lazy"
                      >
                    {% endif %}
                  </div>
                {% endif %}

                {%- assign item_head_tag = section.settings.item_heading_style | default: 'h4' -%}
                <{{ item_head_tag }} class="specs-item-title typography-{{ item_head_tag }}">
                  {{ block.settings.heading }}
                </{{ item_head_tag }}>
              </div>

              {%- assign item_text_tag = section.settings.item_text_style | default: 'p' -%}
              <div class="specs-item-body typography-{{ item_text_tag }}">
                {{ block.settings.text }}
              </div>

            </div>
          {% endfor %}
        </div>

      </div>

    </div>

    {% if section.settings.note_text != blank %}
      {%- assign note_tag = section.settings.note_text_style | default: 'p' -%}
      <div class="specs-bottom-note typography-{{ note_tag }}">
        {{ section.settings.note_text }}
      </div>
    {% endif %}

  </div>

</section>

<style>
/* ==========================================================================
   GLOBAL LAYOUT AND STRUCTURE
   ========================================================================== */
.specs-id-{{ section.id }} {
  position: relative;
  padding: 90px 0;
  background-size: cover !important;
  background-position: center;
  background-repeat: no-repeat !important;
  {% if section.settings.enable_bottom_radius_desktop %}
    border-radius: 0px 0px {{ section.settings.bottom_radius_desktop }}px {{ section.settings.bottom_radius_desktop }}px !important;
  {% else %}
    border-radius: 0px !important;
  {% endif %}
  overflow: hidden;
}

.sonu-specs-section::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,{{ section.settings.overlay_opacity | divided_by: 100.0 }});
  z-index: 1;
}

.sonu-specs-section .page-width {
  position: relative;
  z-index: 2;
}

.specs-main-grid {
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  gap: 80px;
  align-items: start;
}

/* ==========================================================================
   MEDIA / LEFT COLUMN
   ========================================================================== */
.specs-media-col {
  position: relative;
}

.specs-display-img {
  width: 100%;
  display: block;
  height: auto;
  object-fit: contain;
}

/* ==========================================================================
   CONTENT / RIGHT COLUMN
   ========================================================================== */
.specs-content-col {
  color: {{ section.settings.text_color }};
}

.specs-main-title {
  margin-bottom: 50px;
}

.specs-main-title .specs-highlight {
  font-family: var(--font-highlight--family, "Times New Roman", serif);
  font-style: italic;
}

/* ==========================================================================
   DASHBOARD GRID MATRIX
   ========================================================================== */
.specs-features-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 40px 50px;
}

.specs-feature-item {
  position: relative;
}

.specs-item-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 18px;
}

.specs-icon-wrapper {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 22px;
  height: 22px;
  flex-shrink: 0;
}

.specs-icon-wrapper svg {
  width: 22px;
  height: 22px;
  fill: currentColor;
}

.specs-icon-wrapper img {
  width: 22px;
  height: 22px;
  object-fit: contain;
}

.specs-item-title {
  margin: 0;
}

.specs-item-body {
  opacity: .88;
}

.specs-item-body ul {
  margin: 0;
  padding-left: 18px;
}

.specs-item-body li {
  margin-bottom: 8px;
}

.specs-bottom-note {
  grid-column: 1;
  margin-top: 24px;
  color: #fff;
  opacity: .85;
  max-width: 520px;
}

/* ==========================================================================
   ENTRANCE BLUR ANIME INTERFACES
   ========================================================================== */
.animate-blur-spec {
  opacity: 0;
  filter: blur(10px);
  transform: translateY(20px);
  animation: blurFadeSpec 1s ease forwards;
}

.delay-spec-1 { animation-delay: .1s; }
.delay-spec-2 { animation-delay: .2s; }
.delay-spec-3 { animation-delay: .3s; }
.delay-spec-4 { animation-delay: .4s; }
.delay-spec-5 { animation-delay: .5s; }
.delay-spec-6 { animation-delay: .6s; }

@keyframes blurFadeSpec {
  to {
    opacity: 1;
    filter: blur(0);
    transform: translateY(0);
  }
}

/* ==========================================================================
   TABLET BREAKPOINT LAYOUTS
   ========================================================================== */
@media(max-width: 1100px){
  .specs-main-grid {
    grid-template-columns: 1fr;
    gap: 50px;
  }
  .specs-media-col {
    max-width: 720px;
    margin: 0 auto;
  }
}

/* ==========================================================================
   MOBILE BREAKPOINT LAYOUTS
   ========================================================================== */
@media(max-width: 768px){
  .specs-id-{{ section.id }} {
    padding: 70px 0;
    {% if section.settings.enable_bottom_radius_mobile %}
      border-radius: 0px 0px {{ section.settings.bottom_radius_mobile }}px {{ section.settings.bottom_radius_mobile }}px !important;
    {% else %}
      border-radius: 0px !important;
    {% endif %}
  }

  {% if section.settings.mobile_bg_image != blank %}
  .specs-id-{{ section.id }} {
    background-image:url('{{ section.settings.mobile_bg_image | image_url: width: 1200 }}') !important;
  }
  {% endif %}

  .specs-main-grid {
    gap: 40px;
  }

  .specs-main-title {
    margin-bottom: 36px;
  }

  .specs-features-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
  }

  .specs-bottom-note {
    grid-column: 1 / -1;
    margin-top: 40px !important;
    text-align: left;
    max-width: 100%;
  }
}
</style>

{% schema %}
{
  "name": "SONU Specifications",
  "settings": [
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading (Use [[text]] for serif italic)",
      "default": "Tech Specifications"
    },
    {
      "type": "textarea",
      "id": "note_text",
      "label": "Bottom Note"
    },
    {
      "type": "image_picker",
      "id": "left_image",
      "label": "Left Image"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background Image"
    },
    {
      "type": "image_picker",
      "id": "mobile_bg_image",
      "label": "Mobile Background Image"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#4a235e"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay Opacity",
      "min": 0,
      "max": 90,
      "step": 5,
      "default": 20
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Bottom Border Radius"
    },
    {
      "type": "checkbox",
      "id": "enable_bottom_radius_desktop",
      "label": "Enable Bottom Radius (Desktop)",
      "default": true
    },
    {
      "type": "range",
      "id": "bottom_radius_desktop",
      "min": 0,
      "max": 200,
      "step": 5,
      "default": 100,
      "unit": "px",
      "label": "Bottom Radius (Desktop)"
    },
    {
      "type": "checkbox",
      "id": "enable_bottom_radius_mobile",
      "label": "Enable Bottom Radius (Mobile)",
      "default": false
    },
    {
      "type": "range",
      "id": "bottom_radius_mobile",
      "min": 0,
      "max": 150,
      "step": 5,
      "default": 50,
      "unit": "px",
      "label": "Bottom Radius (Mobile)"
    },
    {
      "type": "select",
      "id": "heading_style",
      "label": "Heading Style",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" },
        { "value": "h4", "label": "H4" },
        { "value": "h5", "label": "H5" },
        { "value": "h6", "label": "H6" },
        { "value": "p", "label": "Paragraph (P)" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "h2"
    },
    {
      "type": "select",
      "id": "item_heading_style",
      "label": "Feature Title Style",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" },
        { "value": "h4", "label": "H4" },
        { "value": "h5", "label": "H5" },
        { "value": "h6", "label": "H6" },
        { "value": "p", "label": "Paragraph (P)" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "h4"
    },
    {
      "type": "select",
      "id": "item_text_style",
      "label": "Feature Description Style",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" },
        { "value": "h4", "label": "H4" },
        { "value": "h5", "label": "H5" },
        { "value": "h6", "label": "H6" },
        { "value": "p", "label": "Paragraph (P)" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "p"
    },
    {
      "type": "select",
      "id": "note_text_style",
      "label": "Bottom Note Style",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" },
        { "value": "h4", "label": "H4" },
        { "value": "h5", "label": "H5" },
        { "value": "h6", "label": "H6" },
        { "value": "p", "label": "Paragraph (P)" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "p"
    }
  ],
  "blocks": [
    {
      "type": "spec",
      "name": "Specification",
      "settings": [
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "SVG Icon Code"
        },
        {
          "type": "image_picker",
          "id": "icon_image",
          "label": "Icon Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "What's Included"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "SONU Specifications",
      "blocks": [
        { "type": "spec" },
        { "type": "spec" },
        { "type": "spec" },
        { "type": "spec" }
      ]
    }
  ]
}
{% endschema %}