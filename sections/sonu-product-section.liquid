
{{ 'https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css' | stylesheet_tag }}

{%- assign product = all_products[section.settings.product] -%}

<section class="product-section">
  <div class="page-width">
    <div class="product-grid">
      <!-- LEFT: Product Images -->
      <div class="product-gallery">
        <div class="main-image">
          {% if product.selected_or_first_available_variant.featured_image %}
            <img src="{{ product.selected_or_first_available_variant.featured_image | img_url: 'master' }}" 
                 alt="{{ product.title }}"
                 id="ProductMainImage"
                 loading="lazy">
          {% elsif product.featured_image %}
            <img src="{{ product.featured_image | img_url: 'master' }}" 
                 alt="{{ product.title }}"
                 id="ProductMainImage"
                 loading="lazy">
          {% else %}
            {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
          {% endif %}
        </div>
        
        {% if product.images.size > 1 %}
          <div class="thumbnail-list">
            {% for image in product.images limit: 4 %}
              <div class="thumbnail-item {% if forloop.first %}active{% endif %}" data-image="{{ image.src | img_url: 'master' }}">
                <img src="{{ image.src | img_url: '150x150' }}" alt="{{ product.title }}" loading="lazy">
              </div>
            {% endfor %}
          </div>
        {% endif %}
      </div>

      <!-- RIGHT: Product Info -->
      <div class="product-info">
        {%- assign title_tag = section.settings.title_style | default: 'h1' -%}
        <{{ title_tag }} class="product-title typography-{{ section.settings.title_style | default: 'h2' }}">{{ product.title }}</{{ title_tag }}>
        
        {%- assign desc_tag = section.settings.description_style | default: 'div' -%}
        <{{ desc_tag }} class="product-description typography-{{ section.settings.description_style | default: 'p' }}">
          {{ product.description }}
        </{{ desc_tag }}>
        
        <div class="product-price">
          <span class="current-price typography-{{ section.settings.price_style | default: 'h2' }}" style="{% render 'sonu-typography-style', preset: section.settings.price_style, prefix: 'price_style', settings: section.settings %}">{{ product.selected_or_first_available_variant.price | money }}</span>
          {% if product.selected_or_first_available_variant.compare_at_price > product.selected_or_first_available_variant.price %}
            <span class="compare-price">{{ product.selected_or_first_available_variant.compare_at_price | money }}</span>
          {% endif %}
        </div>
        
        {% if section.settings.fsa_hsa_text != blank %}
          <div class="fsa-hsa-info">
            <span class="fsa-badge">{{ section.settings.fsa_hsa_text }}</span>
          </div>
        {% endif %}
        
        <!-- COLOR & QUANTITY -->
        <div class="options-row">

          <!-- Color -->
          <div class="color-option-box">

            {%- assign option_tag = section.settings.option_label_style | default: 'div' -%}
            <{{ option_tag }} class="option-label typography-{{ section.settings.option_label_style | default: 'h6' }}">Color</{{ option_tag }}>

            <div class="color-swatches" data-option-index="{{ color_option_index }}">
              {% if color_option_index != false %}

                {% assign shown_colors = '' %}

                {% for variant in product.variants %}
                  {% assign color_value = variant.options[color_option_index] %}

                  {% unless shown_colors contains color_value %}
                    {% assign shown_colors = shown_colors | append: color_value | append: ',' %}

                    <div
                      class="color-swatch {% if variant.id == product.selected_or_first_available_variant.id %}active{% endif %}"
                      data-value="{{ color_value }}"
                      data-variant-id="{{ variant.id }}"
                      data-price="{{ variant.price | money }}"
                      data-compare-price="{{ variant.compare_at_price | money }}"
                      data-image="{{ variant.featured_image | default: product.featured_image | img_url: 'master' }}"
                      data-available="{{ variant.available }}"
                    >
                      <span class="swatch-color" style="background:#43345B;"></span>
                    </div>

                  {% endunless %}
                {% endfor %}

              {% else %}

                <!-- Default Single Color -->
                <div class="color-swatch active">
                  <span
                    class="swatch-color"
                    style="background:#4b3b8f;"
                  ></span>
                </div>

              {% endif %}
            </div>

          </div>

          <!-- Quantity -->
          <div class="quantity-option-box">

            <{{ option_tag }} class="option-label typography-{{ section.settings.option_label_style | default: 'h6' }}">Quantity</{{ option_tag }}>

            <div class="quantity-selector">

              <button
                type="button"
                class="quantity-btn minus"
                aria-label="Decrease quantity"
              >
                −
              </button>

              <span class="quantity-number">1</span>

              <button
                type="button"
                class="quantity-btn plus"
                aria-label="Increase quantity"
              >
                +
              </button>

            </div>

          </div>

        </div>
        
        <!-- Stock Status -->
        <div class="stock-status">
          {% if product.selected_or_first_available_variant.available %}
            <span class="in-stock">In stock, ready to ship</span>
          {% else %}
            <span class="out-of-stock">✗ Out of stock</span>
          {% endif %}
        </div>
        
        <!-- Action Buttons -->
        <div class="action-buttons">
          <button type="button" 
                  class="btn btn-primary buy-now typography-{{ section.settings.button_style | default: 'p' }}" 
                  data-variant-id="{{ product.selected_or_first_available_variant.id }}"
                  {% unless product.selected_or_first_available_variant.available %}disabled{% endunless %} style="{% render 'sonu-typography-style', preset: section.settings.button_style, prefix: 'button_style', settings: section.settings %}">
            {{ section.settings.buy_now_text | default: 'BUY NOW' }}
          </button>
          <button type="button" class="btn btn-secondary try-risk-free typography-{{ section.settings.button_style | default: 'p' }}" style="{% render 'sonu-typography-style', preset: section.settings.button_style, prefix: 'button_style', settings: section.settings %}">
            {{ section.settings.try_free_text | default: 'TRY RISK FREE FOR 45 DAYS' }}
          </button>
        </div>
        
        <!-- Features List -->
        {% if section.settings.feature_heading != blank %}
          <div class="features-list">
            <h3 class="features-heading">{{ section.settings.feature_heading }}</h3>
            <ul>
              {% if section.settings.feature_1 != blank %}
                <li>{{ section.settings.feature_1 }}</li>
              {% endif %}
              {% if section.settings.feature_2 != blank %}
                <li>{{ section.settings.feature_2 }}</li>
              {% endif %}
              {% if section.settings.feature_3 != blank %}
                <li>{{ section.settings.feature_3 }}</li>
              {% endif %}
              {% if section.settings.feature_4 != blank %}
                <li>{{ section.settings.feature_4 }}</li>
              {% endif %}
            </ul>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</section>

<style>

.options-row{
  display:flex;
  align-items:center;
  gap:55px;
  margin:30px 0;
}

.color-option-box,
.quantity-option-box{
  display:flex;
  align-items:center;
  gap:16px;
}

.option-label{
  color:#333;
  margin:0;
}

/* COLOR */

.color-swatches{
  display:flex;
  align-items:center;
}

.color-swatch{
  cursor:pointer;
}

.swatch-color{
  width:50px;
  height:50px;
  border-radius:50%;
  display:block;
  border:none;
}

.color-swatch.active .swatch-color{
  box-shadow:
    0 0 0 3px #5d5cff,
    0 0 0 5px #fff;
}

/* QUANTITY */

.quantity-selector{
  width:128px;
  height:54px;
  border:1px solid #b8c6d8;
  border-radius:6px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:0 14px;
  background:#fff;
}

.quantity-btn{
  border:none;
  background:none;
  color:#9aa8b9;
  cursor:pointer;
  width:auto;
  height:auto;
  padding:0;
  line-height:1;
}

.quantity-number{
  color:#111;
  min-width:auto;
  padding:0;
}

.product-section{
  background:#ffffff;
  padding:48px 20px;
}

.product-section .page-width{
  max-width:1200px;
  margin:0 auto;
}

.product-grid{
  display:grid;
  grid-template-columns:520px 1fr;
  gap:48px;
  align-items:start;
}

/* =========================
   PRODUCT GALLERY
========================= */

.product-gallery{
  position:relative;
}

.main-image{
  border-radius:16px;
  overflow:hidden;
  background:#f8f8f8;
  margin-bottom:18px;
}

.main-image img{
  width:100%;
  display:block;
  aspect-ratio:1/1;
  object-fit:cover;
}

.thumbnail-list{
  display:flex;
  gap:12px;
}

.thumbnail-item{
  width:68px;
  height:68px;
  border-radius:0;
  overflow:hidden;
  border:2px solid transparent;
  cursor:pointer;
}

.thumbnail-item.active{
  border-color:#112a73;
}

.thumbnail-item img{
  width:100%;
  height:100%;
  object-fit:cover;
}

/* =========================
   PRODUCT INFO
========================= */

.product-info{
  padding-top:40px;
}

.product-title{
  color:#112a73;
  margin-bottom:24px;
}

.product-description{
  max-width:520px;
  color:#444;
  margin-bottom:30px;
}

.product-description p{
  margin:0;
}

.product-price{
  margin-bottom:8px;
}

.current-price{
  color:#112a73;
}

.compare-price{
  color:#999;
}

/* =========================
   FSA HSA
========================= */

.fsa-hsa-info{
  margin-bottom:34px;
}

.fsa-badge{
  background:none;
  padding:0;
  color:#000;
}

/* =========================
   OPTIONS
========================= */

.options-row{
  display:flex;
  align-items:flex-end;
  gap:40px;
  margin-bottom:34px;
}

.option-label{
  color:#000;
  margin-bottom:10px;
}

.color-swatches{
  gap:10px;
}

.swatch-color{
  width:38px;
  height:38px;
  border-radius:50%;
}

.color-swatch.active .swatch-color{
  box-shadow:
  0 0 0 2px #6c4db7,
  0 0 0 4px #fff;
}

/* =========================
   QUANTITY
========================= */

.quantity-selector{
  border:1px solid #d7dbe7;
  border-radius:4px;
  height:42px;
  background:#fff;
  padding:0;
}

.quantity-btn{
  width:38px;
  height:40px;
  color:#6b7280;
}

.quantity-number{
  width:10px;
  color:#111827;
}

/* =========================
   STOCK
========================= */

.stock-status{
  margin-bottom:34px;
}

.in-stock{
  color:#000;
  display:flex;
  align-items:center;
  gap:8px;
}

.in-stock::before{
  content:'';
  width:8px;
  height:8px;
  border-radius:50%;
  background:#7fb23a;
}

.out-of-stock{
  color:#e53935;
}

/* =========================
   BUTTONS
========================= */

.action-buttons{
  display:flex;
  gap:14px;
}

.btn{
  height:54px;
  border-radius:999px;
  min-width:240px;
}

.btn-primary{
  background:#001755;
  border:1px solid #001755;
  color:#fff;
}

.btn-primary:hover{
  background:#0d2058;
  border-color:#0d2058;
}

.btn-secondary{
  background:#fff;
  border:1px solid #112a73;
  color:#112a73;
}

.btn-secondary:hover{
  background:#f7f8fc;
}

/* =========================
   FEATURES
========================= */

.features-list{
  display:none;
}

/* =========================
   RESPONSIVE
========================= */

@media(max-width:991px){

  .product-grid{
    grid-template-columns:1fr;
    gap:30px;
  }

  .product-info{
    padding-top:0;
  }

  .action-buttons{
    flex-direction:column;
  }

  .btn{
    width:100%;
    min-width:100%;
  }
}

/* =====================================
   MOBILE DESIGN
===================================== */

@media screen and (max-width:767px){

  .product-section{
    padding:30px 20px;
  }

  .product-grid{
    display:flex;
    flex-direction:column;
    gap:20px;
  }

  .product-info{
    padding-top:0;
    text-align:center;
  }

  .product-title{
    margin-bottom:15px;
  }

  .product-description{
    max-width:100%;
    margin-bottom:20px;
  }

  .fsa-hsa-info{
    margin:10px 0 25px;
  }

  /* ==========================
     COLOR + QUANTITY
  ========================== */

  .options-row{
    display:flex;
    flex-direction:column;
    align-items:center;
    justify-content:center;
    gap:28px;
    margin:30px 0;
    width:100%;
  }

  .color-option-box,
  .quantity-option-box{
    display:flex !important;
    flex-direction:column !important;
    align-items:center !important;
    justify-content:center !important;
    width:100%;
    gap:12px;
  }

  .option-label{
    margin:0;
    text-align:center;
  }

  /* Color */

  .color-swatches{
    display:flex;
    justify-content:center;
    align-items:center;
  }

  .swatch-color{
    width:46px;
    height:46px;
    border-radius:50%;
  }

  /* Quantity */

  .quantity-selector{
    width:150px !important;
    height:24px !important;
    display:flex !important;
    flex-direction:row !important;
    align-items:center !important;
    justify-content:space-between !important;
    margin:0 auto !important;
    padding:0 14px !important;
    border:1px solid #cfd8e3;
    border-radius:4px;
    background:#fff;
    box-sizing:border-box;
  }

  .quantity-btn{
    width:28px !important;
    height:30px !important;
    min-width:auto !important;
    padding:0 !important;
    margin:0 !important;
    border:none;
    background:none;
    color:#9aa8b9;
    flex:none !important;
  }

  .quantity-number{
    width:10px !important;
    min-width:auto !important;
    padding:0 !important;
    margin:0 !important;
    font-weight:500;
    color:#111;
    line-height:1;
    flex:none !important;
  }

  /* Stock */

  .stock-status{
    display:flex;
    justify-content:center;
    margin:20px 0 25px;
  }

  .in-stock{
    justify-content:center;
  }

  /* Buttons */

  .action-buttons{
    display:flex;
    flex-direction:column;
    gap:15px;
    width:100%;
  }

  .btn{
    width:100%;
    min-width:100%;
    height:54px;
    border-radius:999px;
  }

  .thumbnail-list{
    justify-content:center;
    gap:8px;
  }

  .thumbnail-item{
    width:55px;
    height:55px;
  }

  .features-list{
    display:none;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Product image thumbnail switcher
  const thumbnails = document.querySelectorAll('.thumbnail-item');
  const mainImage = document.getElementById('ProductMainImage');
  
  thumbnails.forEach(thumb => {
    thumb.addEventListener('click', function() {
      const imageUrl = this.getAttribute('data-image');
      if (imageUrl && mainImage) {
        mainImage.src = imageUrl;
      }
      thumbnails.forEach(t => t.classList.remove('active'));
      this.classList.add('active');
    });
  });
  
  // Quantity selector with number display
  const minusBtn = document.querySelector('.quantity-btn.minus');
  const plusBtn = document.querySelector('.quantity-btn.plus');
  const quantityNumber = document.querySelector('.quantity-number');
  
  if (minusBtn && plusBtn && quantityNumber) {
    let currentQuantity = 1;
    
    minusBtn.addEventListener('click', function() {
      if (currentQuantity > 1) {
        currentQuantity--;
        quantityNumber.textContent = currentQuantity;
      }
    });
    
    plusBtn.addEventListener('click', function() {
      currentQuantity++;
      quantityNumber.textContent = currentQuantity;
    });
  }
  
  // Color swatch selection and variant update
  const colorSwatches = document.querySelectorAll('.color-swatch');
  const priceElement = document.querySelector('.current-price');
  const comparePriceElement = document.querySelector('.compare-price');
  const stockStatusElement = document.querySelector('.stock-status');
  const buyNowBtn = document.querySelector('.buy-now');
  
  colorSwatches.forEach(swatch => {
    swatch.addEventListener('click', function() {
      const variantId = this.getAttribute('data-variant-id');
      const variantPrice = this.getAttribute('data-price');
      const variantComparePrice = this.getAttribute('data-compare-price');
      const variantAvailable = this.getAttribute('data-available') === 'true';
      const variantImage = this.getAttribute('data-image');
      
      // Update active state
      colorSwatches.forEach(s => s.classList.remove('active'));
      this.classList.add('active');
      
      // Update price
      if (priceElement && variantPrice) {
        priceElement.textContent = variantPrice;
      }
      
      // Update compare price
      if (comparePriceElement) {
        if (variantComparePrice && variantComparePrice !== '{{ 0 | money }}') {
          comparePriceElement.textContent = variantComparePrice;
          comparePriceElement.style.display = 'inline';
        } else {
          comparePriceElement.style.display = 'none';
        }
      }
      
      // Update stock status
      if (stockStatusElement) {
        if (variantAvailable) {
          stockStatusElement.innerHTML = '<span class="in-stock">✓ In stock, ready to ship</span>';
        } else {
          stockStatusElement.innerHTML = '<span class="out-of-stock">✗ Out of stock</span>';
        }
      }
      
      // Update buy button
      if (buyNowBtn) {
        buyNowBtn.setAttribute('data-variant-id', variantId);
        if (!variantAvailable) {
          buyNowBtn.disabled = true;
        } else {
          buyNowBtn.disabled = false;
        }
      }
      
      // Update main image
      if (variantImage && mainImage) {
        mainImage.src = variantImage;
      }
    });
  });
  
  // Add to cart / Buy Now functionality
  if (buyNowBtn) {
    buyNowBtn.addEventListener('click', function() {
      const variantId = this.getAttribute('data-variant-id');
      const quantity = parseInt(document.querySelector('.quantity-number')?.textContent || 1);
      
      if (!variantId) {
        console.error('No variant ID found');
        return;
      }
      
      // Add to cart via Shopify AJAX API
      fetch('/cart/add.js', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json'
        },
        body: JSON.stringify({
          id: parseInt(variantId),
          quantity: quantity
        })
      })
      .then(response => response.json())
      .then(data => {
        window.location.href = '/cart';
      })
      .catch(error => {
        console.error('Error adding to cart:', error);
        window.location.href = '/cart/' + variantId + ':' + quantity;
      });
    });
  }
});
</script>

{% schema %}
{
  "name": "Product Section",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Select Product",
      "info": "Choose the product to display"
    },
    {
      "type": "select",
      "id": "title_style",
      "label": "Product Title Style Preset",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        },
        {
          "value": "p",
          "label": "Paragraph (P)"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "h2"
    },
    {
      "type": "range",
      "id": "title_style_font_size_desktop",
      "label": "Desktop Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 32,
      "visible_if": "{{ section.settings.title_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "title_style_font_size_mobile",
      "label": "Mobile Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.title_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "title_style_font_weight",
      "label": "Font Weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        },
        {
          "value": "800",
          "label": "Extra Bold"
        },
        {
          "value": "900",
          "label": "Black"
        }
      ],
      "default": "400",
      "visible_if": "{{ section.settings.title_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "title_style_line_height",
      "label": "Line Height",
      "min": 0.8,
      "max": 2,
      "step": 0.1,
      "default": 1.2,
      "visible_if": "{{ section.settings.title_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "title_style_letter_spacing",
      "label": "Letter Spacing (hundredths of em)",
      "min": -10,
      "max": 50,
      "step": 1,
      "default": 0,
      "visible_if": "{{ section.settings.title_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "title_style_text_transform",
      "label": "Text Transform",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.title_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "description_style",
      "label": "Product Description Style Preset",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        },
        {
          "value": "p",
          "label": "Paragraph (P)"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "p"
    },
    {
      "type": "range",
      "id": "description_style_font_size_desktop",
      "label": "Desktop Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 32,
      "visible_if": "{{ section.settings.description_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "description_style_font_size_mobile",
      "label": "Mobile Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.description_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "description_style_font_weight",
      "label": "Font Weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        },
        {
          "value": "800",
          "label": "Extra Bold"
        },
        {
          "value": "900",
          "label": "Black"
        }
      ],
      "default": "400",
      "visible_if": "{{ section.settings.description_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "description_style_line_height",
      "label": "Line Height",
      "min": 0.8,
      "max": 2,
      "step": 0.1,
      "default": 1.2,
      "visible_if": "{{ section.settings.description_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "description_style_letter_spacing",
      "label": "Letter Spacing (hundredths of em)",
      "min": -10,
      "max": 50,
      "step": 1,
      "default": 0,
      "visible_if": "{{ section.settings.description_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "description_style_text_transform",
      "label": "Text Transform",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.description_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "price_style",
      "label": "Product Price Style Preset",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        },
        {
          "value": "p",
          "label": "Paragraph (P)"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "h2"
    },
    {
      "type": "range",
      "id": "price_style_font_size_desktop",
      "label": "Desktop Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 32,
      "visible_if": "{{ section.settings.price_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "price_style_font_size_mobile",
      "label": "Mobile Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.price_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "price_style_font_weight",
      "label": "Font Weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        },
        {
          "value": "800",
          "label": "Extra Bold"
        },
        {
          "value": "900",
          "label": "Black"
        }
      ],
      "default": "400",
      "visible_if": "{{ section.settings.price_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "price_style_line_height",
      "label": "Line Height",
      "min": 0.8,
      "max": 2,
      "step": 0.1,
      "default": 1.2,
      "visible_if": "{{ section.settings.price_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "price_style_letter_spacing",
      "label": "Letter Spacing (hundredths of em)",
      "min": -10,
      "max": 50,
      "step": 1,
      "default": 0,
      "visible_if": "{{ section.settings.price_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "price_style_text_transform",
      "label": "Text Transform",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.price_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "option_label_style",
      "label": "Option Label Style Preset",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        },
        {
          "value": "p",
          "label": "Paragraph (P)"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "h6"
    },
    {
      "type": "range",
      "id": "option_label_style_font_size_desktop",
      "label": "Desktop Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 32,
      "visible_if": "{{ section.settings.option_label_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "option_label_style_font_size_mobile",
      "label": "Mobile Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.option_label_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "option_label_style_font_weight",
      "label": "Font Weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        },
        {
          "value": "800",
          "label": "Extra Bold"
        },
        {
          "value": "900",
          "label": "Black"
        }
      ],
      "default": "400",
      "visible_if": "{{ section.settings.option_label_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "option_label_style_line_height",
      "label": "Line Height",
      "min": 0.8,
      "max": 2,
      "step": 0.1,
      "default": 1.2,
      "visible_if": "{{ section.settings.option_label_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "option_label_style_letter_spacing",
      "label": "Letter Spacing (hundredths of em)",
      "min": -10,
      "max": 50,
      "step": 1,
      "default": 0,
      "visible_if": "{{ section.settings.option_label_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "option_label_style_text_transform",
      "label": "Text Transform",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.option_label_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "button_style",
      "label": "Button Text Style Preset",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        },
        {
          "value": "p",
          "label": "Paragraph (P)"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "p"
    },
    {
      "type": "range",
      "id": "button_style_font_size_desktop",
      "label": "Desktop Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 32,
      "visible_if": "{{ section.settings.button_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "button_style_font_size_mobile",
      "label": "Mobile Font Size",
      "min": 8,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.button_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "button_style_font_weight",
      "label": "Font Weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Regular"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        },
        {
          "value": "800",
          "label": "Extra Bold"
        },
        {
          "value": "900",
          "label": "Black"
        }
      ],
      "default": "400",
      "visible_if": "{{ section.settings.button_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "button_style_line_height",
      "label": "Line Height",
      "min": 0.8,
      "max": 2,
      "step": 0.1,
      "default": 1.2,
      "visible_if": "{{ section.settings.button_style == 'custom' }}"
    },
    {
      "type": "range",
      "id": "button_style_letter_spacing",
      "label": "Letter Spacing (hundredths of em)",
      "min": -10,
      "max": 50,
      "step": 1,
      "default": 0,
      "visible_if": "{{ section.settings.button_style == 'custom' }}"
    },
    {
      "type": "select",
      "id": "button_style_text_transform",
      "label": "Text Transform",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.button_style == 'custom' }}"
    },
    {
      "type": "text",
      "id": "fsa_hsa_text",
      "label": "FSA/HSA Badge Text",
      "default": "FSA/HSA Eligible"
    },
    {
      "type": "text",
      "id": "buy_now_text",
      "label": "Buy Now Button Text",
      "default": "BUY NOW"
    },
    {
      "type": "text",
      "id": "try_free_text",
      "label": "Try Risk Free Button Text",
      "default": "TRY RISK FREE FOR 45 DAYS"
    },
    {
      "type": "header",
      "content": "Features List"
    },
    {
      "type": "text",
      "id": "feature_heading",
      "label": "Features Heading",
      "default": "Can't Sleep?"
    },
    {
      "type": "text",
      "id": "feature_1",
      "label": "Feature 1",
      "default": "Drug-free solution"
    },
    {
      "type": "text",
      "id": "feature_2",
      "label": "Feature 2",
      "default": "Acoustic resonance therapy"
    },
    {
      "type": "text",
      "id": "feature_3",
      "label": "Feature 3",
      "default": "Clinically tested"
    },
    {
      "type": "text",
      "id": "feature_4",
      "label": "Feature 4"
    }
  ],
  "presets": [
    {
      "name": "Product Section",
      "category": "Product"
    }
  ]
}
{% endschema %}